# Figure sources

All current vector figures are generated directly by
`paper/scripts/generate_figures.py` from the registered evidence manifest or
from explicit architecture/timing constructions. No external image assets are
required. The generated PDFs under `figures/generated/` are the portable
manuscript inputs.
