schema_version: 1
protocol_version: v2
formal: true
run_id: paper-evidence-v2-multidof-plant
output_root: results/paper_evidence_v2/raw_runs
seed: 20260722
multidof:
  dofs: [1, 3, 6, 7, 12]
  patterns: [in_phase, different_frequency, staggered_reversal, one_axis_near_limit, correlated, uncorrelated]
plant_study:
  plants: [ideal, delayed_servo]
  feedback_modes: [previous_command, measured, hybrid]
  bandwidth_hz: [4.0, 8.0, 16.0]
  damping_ratio: [0.7, 1.0]
  delay_ms: [0, 10, 20]
data:
  split_manifest: split_manifest_v2.json
  split: test
  sample_rate_hz: 100
  max_trajectories: 24
pipeline:
  estimator: robust_ca_kf
  predictor: constant_acceleration
  prediction_horizon_ms: 20
  target_mode: pva
  governor: one_step
  follower: direct
  plant: delayed_servo
  measured_state_mode: hybrid
